[project]
name = "streamed-agent-ai-sdk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "chainlit>=2.6.0",
    "load-dotenv>=0.1.0",
    "openai-agents>=0.1.0",
]
